# 📱 Telegram Automation Pro v4.0

**Полностью рабочая система автоматизации Telegram с веб-панелью управления**

---

## ✨ Возможности

- ✅ **Веб-панель управления** - современный интерфейс для управления всеми функциями
- ✅ **Мультиаккаунтность** - работа с несколькими Telegram аккаунтами
- ✅ **Парсинг пользователей** - сбор контактов из групп и каналов
- ✅ **Автоинвайтинг** - автоматическое приглашение пользователей в группы
- ✅ **Рассылка сообщений** - массовая отправка сообщений
- ✅ **Статистика и аналитика** - детальная статистика по всем операциям
- ✅ **Безопасность** - JWT аутентификация, bcrypt хеширование паролей

---

## 🚀 Быстрая установка (одна команда!)

### Требования

- **Windows 10/11**
- **Python 3.11+** ([скачать](https://www.python.org/downloads/))
- **PostgreSQL 15+** ([скачать](https://www.postgresql.org/download/windows/))

### Установка

1. **Распакуйте архив** в любую папку (например, `C:\TelegramAutomation`)

2. **Откройте PowerShell от имени администратора** в папке проекта

3. **Разрешите выполнение скриптов** (если требуется):
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```

4. **Запустите установщик**:
   ```powershell
   .\INSTALL.ps1
   ```

5. **Следуйте инструкциям** установщика:
   - Введите пароль PostgreSQL (по умолчанию: `postgres`)
   - Дождитесь завершения установки

6. **Готово!** Сервер запустится автоматически

---

## 🌐 Доступ к системе

После установки откройте браузер и перейдите по адресу:

**http://localhost:8000/**

### Данные для входа:

- **Имя пользователя**: `admin`
- **Пароль**: `admin123`

---

## 📁 Структура проекта

```
telegram-automation/
├── backend/
│   └── app.py              # Flask API сервер
├── frontend/
│   └── index.html          # Веб-интерфейс
├── database/
│   └── init.sql            # SQL скрипт инициализации БД
├── INSTALL.ps1             # Автоматический установщик
├── requirements.txt        # Python зависимости
├── .env                    # Конфигурация (создаётся при установке)
└── README.md               # Эта инструкция
```

---

## 🔧 Ручная установка (если автоматическая не сработала)

### 1. Создание виртуального окружения

```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

### 2. Установка зависимостей

```powershell
pip install -r requirements.txt
```

### 3. Создание файла .env

Создайте файл `.env` в корне проекта:

```env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=ваш_пароль
DB_NAME=telegram_automation

SECRET_KEY=ваш_секретный_ключ
DEBUG=True

CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
```

### 4. Инициализация базы данных

```powershell
# Создание базы данных
$env:PGPASSWORD="ваш_пароль"
createdb -U postgres telegram_automation

# Выполнение SQL скрипта
psql -U postgres -d telegram_automation -f database\init.sql
```

### 5. Запуск сервера

```powershell
python backend\app.py
```

---

## 🛠️ Управление сервером

### Запуск

```powershell
.\venv\Scripts\Activate.ps1
python backend\app.py
```

### Остановка

Нажмите **Ctrl+C** в окне PowerShell

### Проверка статуса

Откройте в браузере: http://localhost:8000/

---

## 📊 API Endpoints

Все API endpoints доступны по адресу `http://localhost:8000/api/`

### Аутентификация

- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Информация о текущем пользователе

### Панель управления

- `GET /api/dashboard/stats` - Статистика панели управления

### Аккаунты

- `GET /api/accounts` - Список Telegram аккаунтов

### Уведомления

- `GET /api/notifications` - Список уведомлений

---

## 🔒 Безопасность

- Пароли хешируются с использованием **bcrypt**
- Аутентификация через **JWT токены**
- **CORS** настроен только для localhost
- SQL-инъекции предотвращены через **параметризованные запросы**

---

## ❓ Решение проблем

### Ошибка "Python не найден"

Установите Python 3.11+ и убедитесь, что отметили "Add Python to PATH" при установке.

### Ошибка "PostgreSQL не найден"

Установите PostgreSQL 15+ и убедитесь, что служба запущена.

### Ошибка подключения к БД

Проверьте:
1. Запущен ли PostgreSQL: `Get-Service postgresql*`
2. Правильный ли пароль в файле `.env`
3. Существует ли база данных: `psql -U postgres -l`

### Ошибка CORS

Убедитесь, что:
1. В `.env` указаны правильные CORS_ORIGINS
2. Вы обращаетесь к `http://localhost:8000`, а не к другому адресу

### Страница не загружается

1. Проверьте, запущен ли сервер
2. Откройте http://127.0.0.1:8000/ вместо localhost
3. Очистите кеш браузера (Ctrl+Shift+Del)

---

## 📝 Изменение пароля администратора

Выполните в PowerShell:

```powershell
$env:PGPASSWORD="ваш_пароль_postgres"

# Новый пароль: newpassword123
$newHash = '$2b$12$...' # Сгенерируйте новый хеш

psql -U postgres -d telegram_automation -c "UPDATE users SET password_hash = '$newHash' WHERE username = 'admin'"
```

---

## 🆕 Версия 4.0.0 - Что нового?

- ✅ Полностью переписанный backend с правильной CORS конфигурацией
- ✅ Исправлены все ошибки аутентификации
- ✅ Автоматический установщик одной командой
- ✅ Улучшенный веб-интерфейс
- ✅ Полная документация
- ✅ Готов к использованию "из коробки"

---

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Убедитесь, что все требования установлены
3. Проверьте логи в консоли PowerShell

---

## 📄 Лицензия

Этот проект предоставляется "как есть" для личного использования.

---

**Telegram Automation Pro v4.0** - Готовое решение для автоматизации Telegram
